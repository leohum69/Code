/*
Name: Mirza Humayun Masood
Roll No: I22-1749
Class: BS-CY(A)
*/
#include <iostream>

using namespace std;

int main()
{
    float x,y,k,b,h,a,z,g,c; //declaration of variables
    cout<<"Enter x: ";
    cin>>x;
    cout<<"Enter y: ";
    cin>>y;
    cout<<"Enter k: ";
    cin>>k;
    cout<<"Enter b: ";
    cin>>b;
    cout<<"Enter h: ";
    cin>>h;

    a= 12*x;                        //solving expressions

    cout<<"value of a: "<<a<<endl;

    z=(5*x)+(14*y)+(6*k);

    cout<<"value of z: "<<z<<endl;

    y=x*x*x*x;

    cout<<"Value of y: "<<y<<endl;

    g=(h+12)/(4*k);

    cout<<"Value of g: "<<g<<endl;

    c= (a*a*a)/((b*b)*(k*k*k*k));

    cout<<"value of c: "<<c<<endl;








    return 0;
}
